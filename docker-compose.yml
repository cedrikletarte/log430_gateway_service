services:

  # ============================================
  # 🌐 API GATEWAY (Traefik + Gateway Service)
  # ============================================

  traefik:
    image: traefik:3
    container_name: traefik
    command:
      - "--api.dashboard=true"        # Active le dashboard Traefik
      - "--api.insecure=true"          # Dashboard activé en clair (sans authentification)
      - "--providers.docker=true"      # Auto-config à partir des labels Docker
      - "--entrypoints.web.address=:80" # Traefik écoute sur port 80
    ports:
      - "80:80"   # accès à l'API via http://localhost
      - "8080:8080" # Dashboard Traefik http://localhost:8080
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    restart: always
    networks:
      - brokerx-network

  gateway-service:
    build: .
    container_name: gateway-service
    expose:
      - "8080"
    environment:
      GATEWAY_SECRET: ${GATEWAY_SECRET}
      JWT_SECRET: ${JWT_SECRET}
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.gateway.rule=PathPrefix(`/`)" # toutes les requêtes passent au Gateway
      - "traefik.http.routers.gateway.entrypoints=web" # utilise l'entrypoint web (port 80)
      - "traefik.http.services.gateway.loadbalancer.server.port=8080" # port interne du service
      - "traefik.http.services.gateway.loadbalancer.server.scheme=http" # schéma http
    restart: always
    networks:
      - brokerx-network

# ============================================
# 🌐 NETWORKS
# ============================================

networks:
  brokerx-network:
    external: true
    name: brokerx-network
